"""Commands PIAG - Scripts CLI pour les opérations PIAG.

Ce module contient les scripts CLI pour toutes les opérations PIAG.
Chaque fichier peut être exécuté via python -m app.piag.commands.<nom_module>

Les fonctions métier sont exposées via app.piag, pas app.piag.commands.
"""

# Ce module ne doit pas exporter de fonctions.
# Les commands sont des scripts CLI standalone.
# Les fonctions métier sont dans app.piag.core et exposées via app.piag.
