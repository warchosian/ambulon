[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --asyncio-mode=auto
asyncio_mode = auto
markers =
    asyncio: mark test as async
    unit: mark test as unit test (mocked, no network required)
    integration: mark test as integration test (real API calls, network required)
